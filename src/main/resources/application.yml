server:
  port: 8080

spring:
  ai:
    openai:
      api-key: ${DEEPSEEK_API_KEY}   # 你的DeepSeek API密钥
      base-url: https://api.deepseek.com
      chat:
        options:
          model: deepseek-chat